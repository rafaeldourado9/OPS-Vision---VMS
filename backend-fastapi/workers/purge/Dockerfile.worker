FROM python:3.11-slim

RUN apt-get update && apt-get install -y ffmpeg && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY purge/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY purge/worker/ ./worker/
COPY common/ ./common/

CMD ["python", "-m", "worker"]
