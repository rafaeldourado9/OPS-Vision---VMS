# Google Maps API Key (optional — MapPage shows fallback list if not set)
VITE_GOOGLE_MAPS_KEY=

# API base URL (used in settings info display only, proxy handles actual requests)
VITE_API_URL=http://localhost:8000
